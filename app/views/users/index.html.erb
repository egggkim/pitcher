<% if logged_in? %>
  <h1>All users</h1>
  <ul>
    <% @users.each do |u| %>
      <li>
        <%= link_to u.first_name, user_path(u) %>
      </li>
    <% end %>
  </ul>

<% else %>
    <%= render 'shared/links' %>
<% end %>
