<% if logged_in? %>
  <h1>All pitches</h1>
  <ul>
    <% @pitches.each do |p| %>
      <li>
        <%= link_to p.title, p %>
        <br>
        <%= image_tag p.image.url(:small) if p.image.url(:small).present? %>
      </li>
      
    <% end %>
  </ul>
<% end %>
